services:
  app:
    build: .
    container_name: intring-app
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - redis

  redis:
    image: redis:7
    container_name: intring-redis
    ports:
      - "6379:6379"
    restart: unless-stopped

volumes:
  db-data: